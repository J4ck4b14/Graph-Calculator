<Window x:Class="WpfTestApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfTestApp"
        mc:Ignorable="d"
        Title="Calculator" Height="700" Width="1000" Background="#FFF7F7F8">
    <Window.Resources>
        <Style x:Key="CalcButton" TargetType="Button">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="MinWidth" Value="48"/>
            <Setter Property="MinHeight" Value="40"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#FFBDBDBD"/>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="380"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left column: controls -->
        <StackPanel Grid.Column="0" Orientation="Vertical">
            <TextBlock Text="Expression" FontWeight="Bold" Margin="0,0,0,4" />
            <TextBox x:Name="ExpressionTextBox" FontSize="18" Height="44" VerticalContentAlignment="Center" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,8">
                <Button Style="{StaticResource CalcButton}" x:Name="EvalButton" Content="=" Width="62" Click="EvalButton_Click" />
                <Button Style="{StaticResource CalcButton}" x:Name="ClearButton" Content="C" Width="62" Click="ClearButton_Click" />
            </StackPanel>

            <TextBlock Text="Result" FontWeight="Bold" Margin="0,0,0,4" />
            <Border Background="White" BorderBrush="#FFE0E0E0" BorderThickness="1" Padding="6">
                <TextBlock x:Name="ResultTextBlock" FontSize="16" TextTrimming="CharacterEllipsis" />
            </Border>

            <TextBlock Text="Buttons" FontWeight="Bold" Margin="0,12,0,6" />
            <UniformGrid Columns="6" Rows="5">
                <Button Style="{StaticResource CalcButton}" Content="7" Click="AppendButton_Click"/>
                <Button Style="{StaticResource CalcButton}" Content="8" Click="AppendButton_Click"/>
                <Button Style="{StaticResource CalcButton}" Content="9" Click="AppendButton_Click"/>
                <Button Style="{StaticResource CalcButton}" Content="/" Click="AppendButton_Click"/>
                <Button Style="{StaticResource CalcButton}" Content="sin(" Click="AppendButton_Click"/>
                <Button Style="{StaticResource CalcButton}" Content="cos(" Click="AppendButton_Click"/>

                <Button Style="{StaticResource CalcButton}" Content="4" Click="AppendButton_Click"/>
                <Button Style="{StaticResource CalcButton}" Content="5" Click="AppendButton_Click"/>
                <Button Style="{StaticResource CalcButton}" Content="6" Click="AppendButton_Click"/>
                <Button Style="{StaticResource CalcButton}" Content="*" Click="AppendButton_Click"/>
                <Button Style="{StaticResource CalcButton}" Content="tan(" Click="AppendButton_Click"/>
                <Button Style="{StaticResource CalcButton}" Content="sqrt(" Click="AppendButton_Click"/>

                <Button Style="{StaticResource CalcButton}" Content="1" Click="AppendButton_Click"/>
                <Button Style="{StaticResource CalcButton}" Content="2" Click="AppendButton_Click"/>
                <Button Style="{StaticResource CalcButton}" Content="3" Click="AppendButton_Click"/>
                <Button Style="{StaticResource CalcButton}" Content="-" Click="AppendButton_Click"/>
                <Button Style="{StaticResource CalcButton}" Content="(" Click="AppendButton_Click"/>
                <Button Style="{StaticResource CalcButton}" Content=")" Click="AppendButton_Click"/>

                <Button Style="{StaticResource CalcButton}" Content="0" Click="AppendButton_Click"/>
                <Button Style="{StaticResource CalcButton}" Content="." Click="AppendButton_Click"/>
                <Button Style="{StaticResource CalcButton}" Content="," Click="AppendButton_Click"/>
                <Button Style="{StaticResource CalcButton}" Content="+" Click="AppendButton_Click"/>
                <Button Style="{StaticResource CalcButton}" Content="^" Click="AppendButton_Click"/>
                <Button Style="{StaticResource CalcButton}" Content="pi" Click="AppendButton_Click"/>

                <Button Style="{StaticResource CalcButton}" Content="log(" Click="AppendButton_Click"/>
                <Button Style="{StaticResource CalcButton}" Content="ln(" Click="AppendButton_Click"/>
                <Button Style="{StaticResource CalcButton}" Content="exp(" Click="AppendButton_Click"/>
                <Button Style="{StaticResource CalcButton}" Content="pow(" Click="AppendButton_Click"/>
                <Button Style="{StaticResource CalcButton}" Content="abs(" Click="AppendButton_Click"/>
                <Button Style="{StaticResource CalcButton}" Content="back" Click="BackspaceButton_Click"/>
            </UniformGrid>

            <TextBlock Text="Functions" FontWeight="Bold" Margin="0,12,0,6" />
            <StackPanel Orientation="Horizontal">
                <Button Style="{StaticResource CalcButton}" Content="Add" Click="AddFunctionButton_Click" />
                <Button Style="{StaticResource CalcButton}" Content="Remove" Click="RemoveFunctionButton_Click" />
                <Button Style="{StaticResource CalcButton}" Content="Clear" Click="ClearFunctionsButton_Click" />
            </StackPanel>
            <ListBox x:Name="FunctionsListBox" Height="120" Margin="0,6,0,0"/>

            <StackPanel Orientation="Horizontal" Margin="0,12,0,0" VerticalAlignment="Bottom">
                <TextBlock VerticalAlignment="Center" Margin="0,0,8,0">Plot range:</TextBlock>
                <TextBox x:Name="RangeStartTextBox" Width="80" Text="-10" Margin="0,0,4,0" />
                <TextBox x:Name="RangeEndTextBox" Width="80" Text="10" Margin="0,0,8,0" />
                <Button Style="{StaticResource CalcButton}" Content="Plot" Click="PlotButton_Click" />
            </StackPanel>
        </StackPanel>

        <!-- Right column: plot area -->
        <Border Grid.Column="1" Background="White" BorderBrush="#FFE6E6E6" BorderThickness="1" CornerRadius="6" Padding="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Plot" FontWeight="Bold" Margin="0,0,0,8" />
                <Canvas x:Name="PlotCanvas" Grid.Row="1" Background="#FFF9FBFF" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" MinWidth="300" MinHeight="300">
                    <!-- Hover panel that displays current x and list of function values -->
                    <Border x:Name="HoverPanel" Visibility="Collapsed" Background="#CCFFFFFF" BorderBrush="#FFBDBDBD" BorderThickness="1" Padding="6" CornerRadius="4">
                        <StackPanel>
                            <TextBlock x:Name="HoverXText" FontWeight="Bold" />
                            <ListBox x:Name="HoverValuesList" BorderThickness="0" Background="Transparent"/>
                        </StackPanel>
                    </Border>
                </Canvas>
            </Grid>
        </Border>
    </Grid>
</Window>
